# Marketing Report Automation — Codebase Audit Report

_Generated by a 14-dimension multi-agent audit (frontend + backend craft skills). Date: 2026-05-30._

---

## Executive Summary

The repo is in a credible scaffold-to-domain transition: the frontend src/ tree was correctly restructured to a domain-based layout (api/, store/auth/, pages/auth/login/, pages/dashboard/home/, components/auth/, components/theme/, types/) and the backend gained a full layered architecture (routes → controllers → services → schemas → models, MongoDB/Beanie, auth, and a user domain). Frontend structural scores are high (88–96) and the backend error-handling and structure layers are solid. The three top risks are: (1) six primary navigation docs (CLAUDE.md, AGENTS.md, CODEX.md, frontend/CLAUDE.md, frontend/README.md, backend/README.md) still reference the pre-restructure flat layout, actively misdirecting future agents; (2) a plaintext fallback password is committed in config.py, an OWASP A02 violation requiring immediate removal; and (3) a ReDoS vector in the user-list query layer passes raw user-supplied regex to MongoDB with no escaping or length cap. No backend tests exist and the frontend API error class does not conform to the NormalizedApiError contract.

---

## Scorecard

| Dimension | Score | Compliant? | One-line |
|---|---|---|---|
| fe-structure | 91 | Yes | Domain-based src/ layout clean; two inline type-ownership gaps |
| fe-types | 88 | No | RootState/AppDispatch declared inline in store.ts instead of src/types/app/ |
| fe-api | 72 | No | ApiError missing `statusCode`/`isRetryable`; raw fetch TypeError escapes; PageQuery missing `sortBy` |
| fe-state | 96 | Yes | Typed Redux hooks used exclusively; selectors correctly split; one inline prop type |
| fe-styling | 96 | Yes | Tailwind v4 shell correct; all tokens semantic; only cosmetic comment gap |
| fe-quality | 74 | No | 4 orphaned exports; field-name mismatch AuthUser vs SessionUser; both CLAUDE.md docs stale |
| be-structure | 88 | No | WebSocket handler inline in route (skips controller); PaginationMeta defined but unused |
| be-contract | 74 | No | Unknown query params silently ignored; PaginationMeta never used in list endpoint |
| be-errors | 91 | Yes | Centralized handler correct; two low-severity edge cases |
| be-security | 68 | No | Hardcoded fallback password; no bcrypt max-length; no rate limit on login; no security headers |
| be-quality | 74 | No | pydantic BaseModel instead of BaseSettings; ReDoS in regex search; zero backend tests |
| linkage-gaps | 44 | No | 7 stale navigation references across 3 primary agent-read docs; MEMORY.md missing |
| domain-docs | 78 | No | Party Code zero-padding example is factually wrong (13 chars vs 10); internal plant codes under-specified |
| repo-hygiene | 42 | No | 10+ high-severity stale path citations across 6 docs; backend README describes deleted ping-pong scaffold |

**Composite average: 77 / 100**

---

## What Was Set Up This Session

- Frontend src/ restructured from flat layout to domain-based hierarchy: `api/` (base client + domain modules), `store/auth/` (slice + selectors), `pages/auth/login/` + `pages/dashboard/home/`, `components/auth/login/` + `components/theme/`, `types/<domain>/` ownership model, all under 250 lines per file
- `frontend_docs/README.md` generated as a verified orientation hub with the accurate live src/ tree
- `backend_docs/` directory created (empty at time of audit; a `README.md` is recommended but absent)
- Backend restructured from ping-pong scaffold to full layered architecture: MongoDB/Beanie, auth domain (POST /auth/login with bcrypt), user domain (GET /users with pagination + sort whitelist), AppError taxonomy, centralized exception handler, standard success/error envelope
- Project-reference-linkage docs (AGENTS.md, CODEX.md, backend/CLAUDE.md, frontend/CLAUDE.md) created or updated — but all retain pre-restructure FE path references (see Findings)
- Git initialized; .gitignore correctly excludes .env, .venv, __pycache__, node_modules

---

## Findings by Severity

### HIGH

| # | Dimension | File | Issue | Fix |
|---|---|---|---|---|
| H1 | be-security | `backend/app/core/config.py:25` | Hardcoded plaintext fallback password `'loloklol'` committed in source. If `SEED_ADMIN_PASSWORD` is unset, this literal becomes the live admin password (OWASP A02). | Remove the fallback: `os.getenv('SEED_ADMIN_PASSWORD', '')` and raise a startup error if empty. Migrate to `pydantic_settings.BaseSettings`. |
| H2 | linkage-gaps / repo-hygiene | `AGENTS.md:140-146` | Frontend "Where Things Live" table lists 5 paths that do not exist: `features/auth/authSlice.ts`, `lib/api.ts`, `pages/LoginPage.tsx`, `pages/HomePage.tsx`, `components/theme-provider.tsx`. Agents following this table generate broken imports on first write. | Replace with actual paths: `store/auth/slice.ts`, `api/client.ts`, `pages/auth/login/index.tsx`, `pages/dashboard/home/index.tsx`, `components/theme/theme-provider.tsx`. |
| H3 | linkage-gaps / repo-hygiene | `frontend/CLAUDE.md:49-73` | "Actual src/ Structure" tree block shows the pre-restructure layout. All five old paths are absent from disk. Real layout has `api/`, `store/auth/`, `pages/auth/login/`, `pages/dashboard/home/`, `components/theme/`, `types/<domain>/`. | Replace tree block with the canonical layout from `frontend_docs/README.md`. Update lines 94, 129-130, 154 to reference `store/auth/slice.ts` and `api/client.ts`. |
| H4 | linkage-gaps / repo-hygiene | `CODEX.md:27,32,62-63` | Three tables cite `src/lib/api.ts`, `src/features/<domain>/<domain>Slice.ts`, and `src/pages/<Name>Page.tsx` as authoritative conventions — all absent from disk. | Update: API calls → `src/api/client.ts`; slice location → `src/store/<domain>/slice.ts`; pages → `src/pages/<domain>/<route>/index.tsx`. |
| H5 | repo-hygiene | `CLAUDE.md:3,20,60-72` | Root CLAUDE.md (first doc agents load) still calls backend "minimal ping-pong scaffold" with only 4 endpoints and "No DB, no services layer, no auth yet". Real backend has 6 endpoints, MongoDB, auth, and a user domain. | Remove ping-pong framing; replace endpoint table with the real 6-endpoint list; point to `backend/CLAUDE.md` for authority. |
| H6 | repo-hygiene | `CLAUDE.md:78` | States "Only slice present: `src/features/auth/authSlice.ts`". Path absent; real path is `src/store/auth/slice.ts` with companion `src/store/auth/selectors.ts`. | Update to `src/store/auth/slice.ts` + `src/store/auth/selectors.ts`. |
| H7 | repo-hygiene | `CLAUDE.md:94` | States "Backend client: `src/lib/api.ts`". Path does not exist. | Update to `src/api/client.ts` (base client) with domain modules under `src/api/<domain>/`. |
| H8 | repo-hygiene | `CLAUDE.md:126` | Guardrails say "API calls centralized (currently `src/lib/api.ts`, move to `src/api/<domain>/ as domains grow`)". The move already happened. | Update: "API calls centralized in `src/api/client.ts` (base) and `src/api/<domain>/` modules. Domain growth goes into new `src/api/<domain>/` modules." |
| H9 | repo-hygiene | `frontend/README.md:42,49,52-53` | Developer-facing quickstart cites all five old paths in the Structure block and Theming section. | Replace Structure block with current domain-based layout; update Theming section to `src/components/theme/theme-provider.tsx`. |
| H10 | repo-hygiene | `AGENTS.md:54` | States "API calls are centralized in `src/lib/api.ts`" as a must-follow rule. Path absent. | Update: "API calls flow through `src/api/client.ts` (base) and domain modules under `src/api/<domain>/`. Never call fetch in components." |
| H11 | domain-docs | `macro_docs/README.md:31` | Party Code zero-padding example gives `0000040000088` (13 chars) but the stated form is 10-digit. Correct form of `40000088` is `0040000088`. Agents using this produce wrong strip-zero logic. | Replace example: "`0040000088` style / `0000008001`". |

---

### MEDIUM

| # | Dimension | File | Issue | Fix |
|---|---|---|---|---|
| M1 | be-security | `backend/app/schemas/auth.py:14` | Login password field has no `max_length`. Arbitrarily long inputs (e.g. 10 MB) are accepted and passed to bcrypt before truncation — targeted bcrypt-DoS vector (OWASP A07). | Add `max_length=128` to the Field definition. |
| M2 | be-security | `backend/app/routes/auth.py` | No rate limiting on POST /auth/login. Unlimited brute-force / credential-stuffing possible (OWASP A07). | Add slowapi or fastapi-limiter with ~5 requests/minute per IP on the login route. |
| M3 | be-quality | `backend/app/utils/user/query.py:14` | Raw user-supplied `q` param passed verbatim as MongoDB `$regex` with no escaping, anchoring, or length cap. Crafted alternation patterns trigger catastrophic backtracking in mongod (ReDoS / OWASP A05). | Apply `re.escape(query.q)` before the regex value; add `max_length=100` to the `q` field in `UserListQuery`. |
| M4 | be-quality | `backend/app/core/config.py:22-25` | `Settings` uses `pydantic.BaseModel` with `os.getenv()` in field defaults evaluated at class-body execution time (module import), not at instantiation. `lru_cache` compounds the issue. | Add `pydantic-settings` to `requirements.txt`; inherit from `BaseSettings`; remove `os.getenv()` calls and `load_dotenv()`. |
| M5 | fe-api | `frontend/src/api/client.ts:20-32` | `ApiError` class uses `status: number` (should be `statusCode`) and omits `isRetryable: boolean`, violating the `NormalizedApiError` contract required by `frontend-response-handling`. | Rename `status` → `statusCode`; add `isRetryable: boolean` (true for 429/5xx); export `NormalizedApiError` type from `src/types/api/error.ts`. |
| M6 | fe-api | `frontend/src/api/client.ts:35-49` | `fetch()` inside `request()` is not wrapped in try/catch. Network failures throw raw `TypeError` that escapes as an untyped JS error rather than a stable `ApiError`. | Wrap in try/catch; catch `TypeError`/`AbortError` and re-throw as `new ApiError({ code: 'NETWORK_ERROR', message: 'Network request failed' }, 0)`. |
| M7 | be-structure | `backend/app/core/responses.py:20-28` | `PaginationMeta` is defined but never instantiated. `SuccessEnvelope.meta` is typed as `dict[str, Any] \| None`; list service returns a plain dict. Typed model provides zero enforcement. | Change `SuccessEnvelope.meta` to `PaginationMeta \| None`; update `list_users` to return `tuple[list[UserPublic], PaginationMeta]` using a `PaginationMeta` instance. |
| M8 | be-contract | `backend/app/controllers/user.py:13` | Unknown query params on GET /users are silently ignored by FastAPI Depends(). Spec mandates rejection of unrecognised keys. | Inject `Request` alongside Depends(); compute `set(request.query_params.keys()) - ALLOWED_KEYS` and raise 400 ValidationError for any unrecognised key. |
| M9 | be-contract | `backend/app/core/responses.py:20` | `PaginationMeta` is never used; list endpoint returns untyped plain dict for `meta`, making pagination contract invisible to OpenAPI. | Instantiate `PaginationMeta` in `services/user/list.py` and use it as the `meta` value. |
| M10 | fe-quality | `frontend/src/store/auth/selectors.ts:5-8` | Three selectors (`selectAuth`, `selectSessionUser`, `selectAuthToken`) are exported but have zero consumers. Orphaned exports from the restructure. | Remove the three unused selectors; restore individually when a consuming component is introduced. |
| M11 | fe-quality | `frontend/src/types/api/error.ts:16` | `ApiErrorEnvelope` is exported but never imported. Orphaned type. | Delete `ApiErrorEnvelope`; restore if a caller ever needs to type-narrow a raw response envelope. |
| M12 | fe-quality | `frontend/src/types/meta/meta.ts:7` | `RootData` is exported but no `api/meta/root.ts` module exists and no component references it. Orphaned stub. | Delete `RootData`; restore when `api/meta/root.ts` and a consuming component are introduced together. |
| M13 | fe-quality | `frontend/src/components/theme/theme-provider.tsx:35` | `useTheme` is exported from the custom ThemeProvider but has no consumers in any app-owned file. | Remove `export` keyword (make module-private) until a UI component needs theme toggling. |
| M14 | linkage-gaps | `CODEX.md:75` | "Memory Bootstrap" section references `MEMORY.md` in the project root — file does not exist on disk. | Either create `MEMORY.md` as a stub, or remove the reference and note that session memory is tracked via Memory MCP entities. |
| M15 | linkage-gaps | `AGENTS.md:126` | Backend "Where Things Live" table describes `app/main.py` as the file with "all routes". After restructure, `main.py` is a `create_app()` factory; routes are in `routes/__init__.py` and domain router files. | Update to list the layered architecture: `routes/__init__.py` (aggregator), `routes/{meta,auth,user}.py`, `controllers/`, `services/`, `schemas/`, `models/`, `core/`. |
| M16 | domain-docs | `macro_docs/README.md:32` | Join-key section lists internal codes as only "8451-8499 / 8001-style". ZSD also contains 3-4 digit plant codes (1202, 1441, 1478) not in those ranges — agents will not exclude them when joining. | Expand to: "Short codes — 8451–8499, 8001-style, or any code below 5 digits (e.g. 1202, 1441, 1478) — are internal JSW plants. Filter out before joining." |
| M17 | repo-hygiene | `CODEX.md:15` | Architecture Decision from 2026-05-29 cites `src/features/auth/authSlice.ts` with no superseded-by annotation. No FE restructure entry exists for 2026-05-30. | Add 2026-05-30 FE restructure entry; append "(superseded — see 2026-05-30 entry)" to line 15. |
| M18 | repo-hygiene | `backend/README.md:1-38` | Still describes the original ping-pong scaffold. Backend was fully restructured with layered architecture, MongoDB, auth, and user domains. | Rewrite to match `backend/CLAUDE.md`: remove "ping-pong" framing, list all 6 endpoints, document layered directory structure, add MongoDB/Beanie and bcrypt dependencies. |
| M19 | repo-hygiene | `README.md:29` | States the home screen has a "Ping-Pong API card" — AGENTS.md explicitly marks this as absent from the frontend. | Replace with accurate description of what home screen currently shows (a protected placeholder). |
| M20 | fe-types | `frontend/src/app/store.ts:10-11` | `RootState` and `AppDispatch` declared inline in the store configuration file. Store-facing contracts must live in `src/types/<domain>/`, not beside implementation. | Create `src/types/app/store.ts` exporting `RootState` and `AppDispatch`; update imports in `src/app/hooks.ts` and selectors. |
| M21 | be-quality | `backend/` | Zero test files exist. No pytest setup, no conftest.py, no coverage of any service, controller, route, or utility. | Create `backend/tests/` with conftest.py (mongomock-motor or testcontainers), FastAPI TestClient. Add tests for login service (correct credentials, wrong password, unknown email) and list_users (pagination, sort whitelist, q filter). |

---

### LOW

| # | Dimension | File | Issue | Fix |
|---|---|---|---|---|
| L1 | be-security | `backend/app/main.py:54-59` | No security response headers (missing `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, CSP). CORS uses `allow_methods=['*']` and `allow_headers=['*']` with `allow_credentials=True`. | Add thin Starlette middleware injecting the four standard headers. Narrow CORS to `['GET','POST']` and `['Content-Type','Authorization']`. |
| L2 | be-errors | `backend/app/core/exception_handlers.py:42` | `str(exc.detail)` on a dict-valued Starlette `HTTPException.detail` produces Python dict repr visible to clients. | Guard: `exc.detail if isinstance(exc.detail, str) else 'An error occurred'`. |
| L3 | be-errors | `backend/app/core/errors.py` | No `RateLimitError` (429) subclass in AppError taxonomy — incomplete per skill's status-code rules. | Add `class RateLimitError(AppError): status_code = 429; code = 'RATE_LIMITED'`. |
| L4 | be-structure | `backend/app/routes/meta.py:28-35` | WebSocket handler body inline in route file; skips the route→controller→service chain. | Extract handler into `controllers/meta.py`; route calls `ws_ping_controller(websocket)`. |
| L5 | be-contract | `backend/app/schemas/user.py:20` | `UserListQuery` overrides `sortOrder` to `'asc'` while `PageQuery` base and both API skills specify `'desc'` as standard default. Undocumented override. | Remove the override (inherit `'desc'`), or add an inline comment justifying ascending order for alphabetical user listing. |
| L6 | be-quality | `backend/app/utils/user/query.py:8` | `build_user_filter` return type is bare `dict`; `list_users` returns `tuple[list[UserPublic], dict]` instead of `PaginationMeta`. | Annotate as `dict[str, Any]`; return `PaginationMeta` from list service; update return types end-to-end. |
| L7 | fe-api | `frontend/src/types/api/envelope.ts:29-33` | Base `PageQuery` is missing `sortBy`. Any future list domain extending it will silently omit `sortBy` from the contract. | Add `sortBy?: string` and `q?: string` to `PageQuery` as base defaults; domain interfaces can narrow them. |
| L8 | fe-quality | `frontend/src/types/auth/auth.ts:9` | Field name mismatch: `LoginCredentials` and `AuthUser` use `emailid`; `SessionUser` uses `email`. Latent compile error when mock auth is replaced with real `login()` call. | Either rename `SessionUser.email` → `emailid`, or add an explicit adapter mapping `AuthUser { emailid }` → `SessionUser { email }`. |
| L9 | fe-quality | `frontend/CLAUDE.md:53` (and root CLAUDE.md) | Both CLAUDE.md docs still document old flat src/ layout, violating the dead-code-and-change-audit doc-cleanup rule. | Update src/ tree sections in both files to reflect the current domain-based layout. |
| L10 | fe-structure | `frontend/src/components/theme/theme-provider.tsx:16` | Inline prop type literal `{ children: ReactNode }` on function signature instead of a named interface in `src/types/theme/`. | Add `export interface ThemeProviderProps { children: ReactNode }` to `src/types/theme/theme.ts`; import and use it. |
| L11 | fe-state | `frontend/src/components/theme/theme-provider.tsx:16` | Same inline prop type (overlaps L10 — cross-referenced from fe-state audit). | Same fix as L10. |
| L12 | fe-structure | `frontend/src/components/auth/login/hooks/useLoginForm.ts:23` | Anonymous structural type cast `location.state as { from?: { pathname: string } } \| null` inline in hook body. Belongs in `src/types/auth/login-ui.ts`. | Add `export interface LoginRouteState { from?: { pathname: string } }` to `src/types/auth/login-ui.ts`; use it in the cast. |
| L13 | fe-styling | `frontend/src/index.css:1-4` | `tw-animate-css` and `shadcn/tailwind.css` imports may have overlapping keyframe definitions; purpose of each import is not documented. | Add one-line comments above each import. Audit whether both are still needed when a domain animation is introduced. |
| L14 | domain-docs | `macro_docs/zsd-currstk-hr.md:17` | Available-to-sell note says "Usage Decision in ACCEPT/PRIME" but column has three accepted values: ACCEPT, ACCEPTED, PRIME. | Update note to: "Usage Decision in {ACCEPT, ACCEPTED, PRIME}". |
| L15 | linkage-gaps | `backend_docs/` (directory) | No `backend_docs/README.md` exists. `frontend_docs/README.md` has no backend equivalent, leaving backend sessions without a verified orientation hub. | Create `backend_docs/README.md` mirroring `frontend_docs/README.md` style: layered layout, endpoints, AppError taxonomy, envelope contract, seed/migration CLI. Add to CLAUDE.md Pointers table. |
| L16 | linkage-gaps | `CLAUDE.md:135-144` (Pointers table) | `frontend_docs/README.md` — the only doc with an accurate src/ layout — is not referenced in the Pointers table. Agents following the standard read order never discover it. | Add row: `frontend_docs/README.md \| Verified frontend src/ orientation hub — accurate stack, layout tree, API layer map, and golden rules`. |
| L17 | repo-hygiene | `.gitignore:18` | `!.env.example` negation is dead weight — CODEX.md confirms `.env.example` was removed. | Remove the `!.env.example` line. |
| L18 | repo-hygiene | `frontend/vite.config.ts:17` | Inline comment reads "Proxy API calls to the FastAPI ping-pong backend during dev." | Update: "Proxy API calls to the FastAPI backend during dev. Strips /api prefix before forwarding to :8000." |
| L19 | repo-hygiene | `CODEX.md:30` | Design tokens row cites `src/components/theme-provider.tsx`. | Update to `src/components/theme/theme-provider.tsx`. |

---

## Prioritized Remediation Checklist

> Items are ordered by risk. Items marked [BLOCKING] should be resolved before any new domain work begins.

### Immediate (Security / Data Correctness)

- [ ] **[BLOCKING — H1]** Remove hardcoded fallback password from `backend/app/core/config.py:25`. Migrate to `pydantic_settings.BaseSettings`. Rotate the admin password in all environments.
- [ ] **[M3]** Apply `re.escape()` to user-supplied `q` param in `backend/app/utils/user/query.py` before MongoDB `$regex`; add `max_length=100` to `q` field in `UserListQuery`.
- [ ] **[M1]** Add `max_length=128` to the login password field in `backend/app/schemas/auth.py`.
- [ ] **[M2]** Add per-IP rate limiting (≤5/min) to `POST /auth/login`.

### High Priority (Agent Navigation — do these before the next coding session)

> **Root CODEX.md / frontend/CLAUDE.md / AGENTS.md / CLAUDE.md still reference pre-restructure FE paths (`src/features/auth/authSlice.ts`, `src/lib/api.ts`, `src/pages/LoginPage.tsx`, etc.) that do not exist on disk. Every agent reading these docs will produce broken imports. Fix all stale references before spawning any new coding agent.**

- [ ] **[H2, H3, H4]** Update `AGENTS.md` frontend table, `frontend/CLAUDE.md` src/ tree block, and `CODEX.md` patterns/naming tables to use the real restructured paths.
- [ ] **[H5, H6, H7, H8]** Update root `CLAUDE.md`: remove ping-pong framing; fix auth slice path; fix API client path; fix guardrails rule.
- [ ] **[H9, H10]** Update `frontend/README.md` Structure block and `AGENTS.md` Must-Follow rule for API centralization.
- [ ] **[H11]** Fix Party Code zero-padding example in `macro_docs/README.md` (`0000040000088` → `0040000088`).
- [ ] **[L16]** Add `frontend_docs/README.md` to the CLAUDE.md Pointers table so agents discover it in the standard read order.
- [ ] **[M14]** Create `MEMORY.md` stub at project root or remove the reference from CODEX.md.

### Medium Priority (Contract / Quality)

- [ ] **[M5, M6]** Fix `ApiError` class: rename `status` → `statusCode`, add `isRetryable`, export `NormalizedApiError` type from `src/types/api/error.ts`. Wrap raw `fetch()` in try/catch.
- [ ] **[M7, M9, L6]** Instantiate `PaginationMeta` in `services/user/list.py` instead of returning a plain dict; change `SuccessEnvelope.meta` type to `PaginationMeta | None`.
- [ ] **[M8]** Add unknown-query-param rejection guard to GET /users controller.
- [ ] **[M4]** Switch `Settings` to `pydantic_settings.BaseSettings`; add `pydantic-settings` to `requirements.txt`.
- [ ] **[M10, M11, M12, M13]** Remove 4 orphaned exports: `selectAuth`, `selectSessionUser`, `selectAuthToken` (selectors.ts); `ApiErrorEnvelope` (types/api/error.ts); `RootData` (types/meta/meta.ts); `useTheme` export keyword (theme-provider.tsx).
- [ ] **[M21]** Create `backend/tests/` with conftest.py and initial test coverage for login service and list_users service.
- [ ] **[M18, M19]** Rewrite `backend/README.md` to match real backend; remove Ping-Pong API card claim from root `README.md`.

### Lower Priority (Hygiene / Polish)

- [ ] **[L1]** Add security response headers middleware; narrow CORS `allow_methods`/`allow_headers`.
- [ ] **[L4]** Extract WebSocket handler from `routes/meta.py` into `controllers/meta.py`.
- [ ] **[L3]** Add `RateLimitError` (429) to `core/errors.py` to complete AppError taxonomy.
- [ ] **[M20]** Move `RootState` / `AppDispatch` to `src/types/app/store.ts`.
- [ ] **[L7]** Add `sortBy?: string` and `q?: string` to base `PageQuery` interface.
- [ ] **[L8]** Resolve `emailid` vs `email` field name mismatch between `AuthUser` and `SessionUser`.
- [ ] **[L10, L12]** Inline prop types → named interfaces in `src/types/theme/theme.ts` and `src/types/auth/login-ui.ts`.
- [ ] **[L15]** Create `backend_docs/README.md` as a verified backend orientation hub.
- [ ] **[M16]** Expand internal plant-code filter guidance in `macro_docs/README.md` to include sub-5-digit codes.
- [ ] **[L5]** Document or remove undocumented `sortOrder: 'asc'` override in `UserListQuery`.
- [ ] **[L2, L14, L17, L18, L19]** Minor doc/code comment cleanups (Starlette detail guard, zsd-currstk note, .gitignore dead rule, vite.config comment, CODEX.md theme-provider path).

---

## Known / Accepted

- **Mock authentication**: the frontend login accepts any non-empty credentials and persists to `localStorage`. This is an accepted scaffold-phase decision; replace with real JWT flow when auth is wired end-to-end.
- **12 pre-existing shadcn-only ESLint errors**: `react-refresh/only-export-components` warnings originate entirely from generated shadcn/ui components under `src/components/ui/`. These are upstream library files and are not app-owned code; they are intentionally excluded from the quality assessment.
- **JSW brand tokens (`--jsw-blue`, `--jsw-red`, `--jsw-steel`) not yet defined**: `index.css` uses shadcn/ui oklch tokens. The brand token set is noted as not yet defined in `CLAUDE.md` and is deferred until the first dashboard screen is designed.
- **No DB migration tooling**: Beanie/MongoDB requires no schema migrations; seeding is via `seeder.py`. Accepted for the current document-store use case.
- **`openpyxl` / `pandas` not in `requirements.txt`**: intentionally absent until the first SAP Excel ingestion endpoint is built; documented as a gotcha in `CLAUDE.md` and `macro_docs/README.md`.
