# Live local configuration for the Branch Wise Report Automation API.
# Override per-environment; do not commit real production secrets.

# MongoDB connection
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=marketing_report

# Seed admin (created on startup / via `python -m app.scripts.seed`)
SEED_ADMIN_EMAIL=ajayirkal@docketrun.com
SEED_ADMIN_PASSWORD=loloklol

# Session cookie signing secret (httpOnly auth)
SESSION_SECRET=Tq_rPo1GP1Y3VFPVXgfN8N-KGNF6IjnjsbH2VtNBHl-zyOKBfF1QXHeXb8HAzo8i

# --- Mailjet SMTP relay (OTP / invite emails) ---
# Leave SMTP_HOST empty to keep the dev-log fallback (emails printed to console).
# Fill these three from Mailjet → Account settings → SMTP and SEND API settings,
# then restart the server. SMTP_FROM must be a Mailjet-VALIDATED sender/domain.
SMTP_HOST=in-v3.mailjet.com
SMTP_PORT=587
SMTP_STARTTLS=true
SMTP_SSL=false
SMTP_USER=7a3b5821bb36be1f5d1748c721bb2bc2
SMTP_PASSWORD=77248d130332e189df7884044ebde325
SMTP_FROM=ajayirkal@docketrun.com
