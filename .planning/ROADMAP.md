# Roadmap: Marketing Report Automation

> **INITIAL DRAFT** — This roadmap was auto-generated during brownfield scaffold initialization on 2026-05-29.
> Refine phase goals, success criteria, and sequencing by running `/gsd:new-project` or `/gsd:discuss-phase`.

---

## Overview

The project moves in five phases from the current minimal scaffold (ping API + dashboard shell + data dictionary) through a fully operational reporting system. Phases 1–2 lay the data foundation; Phases 3–4 deliver the user-facing value; Phase 5 hardens the system for production use. Each phase is independently deliverable.

---

## Progress

| Phase | Name | Status | Notes |
|-------|------|--------|-------|
| 1 | Foundations & scaffolding | 🟡 In progress | Backend scaffold done; FE shell done; data dictionary done |
| 2 | Excel ingestion layer | ⬜ Not started | Blocked on ZSD invalid-XML fix |
| 3 | Report / query API | ⬜ Not started | Depends on Phase 2 |
| 4 | Dashboard reports & visualizations | ⬜ Not started | Depends on Phase 3 |
| 5 | Auth & hardening | ⬜ Not started | Can partially overlap Phase 4 |

---

## Phases

### Phase 1 — Foundations & Scaffolding

> Largely **DONE**. Covers everything already in the codebase.

- [x] FastAPI ping-pong service (`GET /`, `GET /health`, `GET /ping`, `WS /ws/ping`)
- [x] Python 3.13 venv + pinned `requirements.txt`
- [x] Vite 8 + React 19 + TypeScript frontend with JSW brand tokens
- [x] Redux Toolkit auth slice with mock authentication and `ProtectedRoute`
- [x] shadcn/ui component library (~55 components)
- [x] Data dictionary (`macro_docs/`) for all three SAP Excel files
- [x] CORS configuration for local dev/preview ports
- [ ] Ping feature wired end-to-end in the dashboard _(README describes this but the slice/component does not yet exist in `src/` — see codebase-map.md)_

**Success criteria:**
- `./backend/run.sh` starts without errors; `GET /health` returns `{"status": "ok"}`
- `npm run dev` in `frontend/` opens the dashboard; login with any non-empty credentials succeeds
- `macro_docs/README.md` routes all three file types to the correct per-file doc

**Plans:** _(none — already implemented)_

---

### Phase 2 — Excel Ingestion Layer

- [ ] Tolerant reader for `credit report.XLSX` (skip `#VALUE!` errors, drop trailing blank rows, strip header trailing spaces)
- [ ] Tolerant reader for `west  central customer codes.xlsx` (drop unnamed junk columns, normalise casing)
- [ ] Tolerant reader for `ZSD_CURRSTK_HR.xlsx` using raw-ZIP parse (handle invalid `"1.057.000"` XML cell; keep non-floatable cells as strings)
- [ ] Normalised Python dataclass / Pydantic models for each file
- [ ] Cast TEXT numeric columns (`Act.Thickness (mm)`, `Width (mm)`, `Length(mm)`, `HARDNESS`, `YIELD STRENGTH`, `UTS`) before storage
- [ ] Join-key utility: strip leading zeros from ZSD `Party Code`; exclude internal yard codes (8451–8499, 8001-style)
- [ ] Unit tests for each reader covering the known gotchas
- [ ] `pip install openpyxl` (and `pandas` if used) added to setup docs

**Success criteria:**
- All three files load without errors from a fresh Python environment
- ZSD reader handles the `"1.057.000"` cell gracefully (string, not crash)
- Customer-code join produces correct matches for a known customer (e.g. `40000088`)
- No `#VALUE!` errors surface in the credit model

**Plans:** `phases/phase-2-excel-ingestion/PLAN.md` _(to be created)_

---

### Phase 3 — Report / Query API

- [ ] Credit endpoints: credit limit, exposure, overdue, block status per customer
- [ ] Customer endpoints: CAM, contact, segment, route/destination lookup
- [ ] Stock endpoints: inventory by customer/grade/dimension; aging; NCO/rework flags
- [ ] Joined query: given a customer code, return credit + account + live stock in one response
- [ ] API response envelope: `{success, data, message, meta}` for success; `{success: false, error: {code, message, details}}` for errors
- [ ] Pagination: `page=1`, `limit=20`, `max=100`, `sortBy`, `sortOrder`; whitelist sort keys per endpoint
- [ ] OpenAPI docs auto-generated by FastAPI

**Success criteria:**
- `GET /api/customers/{code}/summary` returns joined data for a known customer code
- All list endpoints paginate correctly; sort-key whitelist rejects invalid keys with 400
- Response envelope is consistent across all endpoints

**Plans:** `phases/phase-3-report-api/PLAN.md` _(to be created)_

---

### Phase 4 — Dashboard Reports & Visualizations

- [ ] Replace placeholder `HomePage` card with real report widgets
- [ ] Credit exposure overview chart (recharts)
- [ ] Stock aging / inventory distribution chart (recharts)
- [ ] Customer search with server-driven filtering, sorting, and pagination (no client-side filter)
- [ ] Drill-down: customer → credit + stock joined view
- [ ] Responsive layout; define + use JSW brand tokens (`--jsw-blue` / `--jsw-red` / `--jsw-steel`) — not yet present in `index.css`
- [ ] Frontend API layer in `src/lib/api.ts` extended for all new endpoints
- [ ] All API calls centralized (no fetch calls inside components)
- [ ] Types in `src/types/<domain>/`

**Success criteria:**
- Dashboard loads real data from the Phase 3 API; no mock/hardcoded values
- Charts render correctly for a known customer sample
- Filtering and pagination are server-driven; network tab shows correct query params
- No component file exceeds 250 lines

**Plans:** `phases/phase-4-dashboard/PLAN.md` _(to be created)_

---

### Phase 5 — Auth & Hardening

- [ ] Replace mock auth (`localStorage` bypass) with real JWT or session-based authentication
- [ ] Backend auth endpoints: `POST /auth/login`, `POST /auth/logout`, token refresh
- [ ] `ProtectedRoute` validates real token; expired tokens redirect to `/login`
- [ ] OWASP security pass: input validation, rate limiting, secure headers
- [ ] Remove hardcoded CORS origins; move to environment config
- [ ] `VITE_API_URL` and all secrets moved to `.env` (not committed)
- [ ] Production build verified: `npm run build` + `vite preview` smoke test

**Success criteria:**
- Empty credentials are rejected by the API (not just the frontend)
- Token expiry forces re-login without data leakage
- `npm run build` produces a clean bundle; `vite preview` serves the app correctly
- No secrets in source control

**Plans:** `phases/phase-5-auth-hardening/PLAN.md` _(to be created)_
