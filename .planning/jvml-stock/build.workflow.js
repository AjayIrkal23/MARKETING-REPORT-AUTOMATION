export const meta = {
  name: 'jvml-stock-build',
  description: 'Build JVML Stock feature end-to-end: 35 parallel builders create all jvml-namespaced files (orchestrator wires shared files by hand)',
  phases: [
    { title: 'Build', detail: '35 parallel builders create backend + frontend jvml files' },
  ],
}

const ROOT = '/DATA/CODE_FILES/MARKETING REPORT AUTOMATION'
const SPEC = `${ROOT}/.planning/jvml-stock/SPEC.md`
const ADDENDUM = `${ROOT}/.planning/jvml-stock/ADDENDUM.md`
const NOTES = `${ROOT}/.planning/jvml-stock/notes`

const GUARD = `HARD RULES — read carefully:
- Feature namespace is jvml_stock / jvml-stock / JvmlStock ONLY. A SEPARATE Claude session is building JSW Stock concurrently in this SAME working tree.
- Create ONLY the file(s) explicitly assigned to you. Do NOT create or edit any other file.
- NEVER touch any jsw_stock / jsw-stock / JswStock file, and NEVER touch .planning/jsw-stock/.
- NEVER edit these SHARED files (the orchestrator wires them by hand): backend app/models/__init__.py, app/core/database.py, app/core/scheduler.py, app/routes/__init__.py, app/services/audit/events.py, app/models/audit_log.py, app/schemas/audit_log.py, app/services/audit_log/options.py, requirements.txt; frontend src/api/client.ts, src/components/layout/nav-items.ts, src/App.tsx, src/pages/admin/settings/index.tsx, src/types/admin/audit-log.ts, src/components/admin/audit-logs/AuditCategoryBadge.tsx.
- AUTHORITATIVE SOURCES (read in this order): ${ADDENDUM} (corrections + consolidated field lists WIN over everything — copy field lists VERBATIM), then ${SPEC}, then your notes file(s) under ${NOTES}/, then your reference file(s).
- Contract: backend JSON uses success() envelope {success,data,message,meta}; Literal sort whitelist; unknown-query-key rejection via frozenset; re.escape on all $regex; typed AppError. Frontend: server-driven only (no client filtering); semantic shadcn tokens; reuse shadcn ui primitives.
- EVERY file MUST be <=250 lines. Full type annotations. Match the surrounding code's style exactly.
- You MAY assume files created by sibling builders exist (e.g. types, api, services) — they are written in parallel; cross-imports resolve at typecheck/import time, not now. putData exists in client.ts (orchestrator adds it).
After writing, return: the exact file path(s) you created and the line count of each.`

const T = [
  // ---------------- BACKEND (20 builders, 24 files) ----------------
  { k: 'be-model-stock', files: ['backend/app/models/jvml_stock.py'], notes: ['be-model'], refs: ['backend/app/models/customer_code.py'],
    job: 'Create the JvmlStock Beanie Document (collection "jvml_stock"). Copy the model field block VERBATIM from ADDENDUM (all 72 data fields + 7 meta fields, Indexed() per SPEC §1). _now_utc default factory; class Settings name="jvml_stock". Terse 1-line docstrings to stay <=250 lines.' },
  { k: 'be-model-config-ingestion', files: ['backend/app/models/jvml_stock_config.py','backend/app/models/jvml_stock_ingestion.py'], notes: ['be-model'], refs: ['backend/app/models/region.py'],
    job: 'Create JvmlStockConfig (collection "jvml_stock_configs": key Indexed(unique=True)="default", enabled, base_path, file_name, start_time="08:00", end_time="20:00", interval_hours=1, notify_emails list, created_at, updated_at) and JvmlStockIngestion (collection "jvml_stock_ingestions": report_date Indexed(unique=True), status, row_count=0, file_path, found_at, alerted_at, error, created_at, updated_at). Per ADDENDUM/SPEC §2.' },
  { k: 'be-schema-list', files: ['backend/app/schemas/jvml_stock.py'], notes: ['be-schema'], refs: ['backend/app/schemas/customer_code.py'],
    job: 'Create JvmlStockSortBy + JvmlStockField Literals (exact per SPEC/ADDENDUM) and JvmlStockListQuery(PageQuery) (sortBy default "created_at", q max_length=200, the 10 filter fields str|None max_length=200, dateFrom/dateTo str|None) and JvmlStockOptionsQuery(field: JvmlStockField, q, limit 1..50 default 20).' },
  { k: 'be-schema-record', files: ['backend/app/schemas/jvml_stock_record.py'], notes: ['be-schema'], refs: ['backend/app/schemas/customer_code.py'],
    job: 'Create JvmlStockPublic(BaseModel): id:str + ALL 72 fields (text->str|None, number->float|None, date->datetime|None) + party_code_normalized, customer_name, customer_code_id (str|None), report_date:str, source_file:str|None, created_at:datetime, updated_at:datetime. Copy the field block VERBATIM from ADDENDUM.' },
  { k: 'be-schema-config', files: ['backend/app/schemas/jvml_stock_config.py'], notes: ['be-schema'], refs: ['backend/app/schemas/region.py'],
    job: 'Create JvmlStockConfigPublic, JvmlStockConfigUpdate (with pydantic v2 field_validators: strip base_path; file_name reject / \\\\ ..; start_time/end_time regex ^([01]\\\\d|2[0-3]):[0-5]\\\\d$; notify_emails list[EmailStr] lowercase+dedupe+max 100 mirroring region), JvmlStockStatusPublic, JvmlStockIngestionRow. Per SPEC §2.' },
  { k: 'be-columns', files: ['backend/app/utils/jvml_stock/__init__.py','backend/app/utils/jvml_stock/columns.py'], notes: ['be-columns-excel'], refs: [],
    job: 'Create utils/jvml_stock/__init__.py (empty) and columns.py: COLUMNS list copied VERBATIM from ADDENDUM (all 72 (header,field,type)); HEADER_TO_FIELD keyed by normalize_header; FIELD_TYPES; TEXT_FIELDS/NUMBER_FIELDS/DATE_FIELDS; normalize_header(h); coerce_value(field,raw) (text/number/date rules per SPEC); excel_serial_to_datetime(serial)=datetime(1899,12,30)+timedelta(days=serial). Data-only, <=250 lines.' },
  { k: 'be-excel', files: ['backend/app/utils/jvml_stock/excel.py'], notes: ['be-columns-excel'], refs: ['backend/app/utils/customer_code/excel.py'],
    job: 'Create excel.py parse_workbook(data: bytes)->list[dict[str,object]] using stdlib zipfile + xml.etree.ElementTree ONLY (NOT openpyxl). Read xl/sharedStrings.xml (concat all <t> per <si>); parse xl/worksheets/sheet1.xml; resolve each <c> by t attr (s->shared, inlineStr-><is><t>, str/b-><v>, numeric-> float(v) if parseable else raw string); col_index from ref letters; row1=header -> index_to_field via HEADER_TO_FIELD (drop unmapped/blank); return raw {field:value} per data row; skip empty rows; strip XML namespace. <=250 lines.' },
  { k: 'be-query', files: ['backend/app/utils/jvml_stock/query.py'], notes: ['be-services-core'], refs: ['backend/app/utils/customer_code/query.py'],
    job: 'Create query.py build_jvml_stock_filter(query) (q $or over the 10 q-fields with re.escape; 10 per-field exact matches; created_at $gte dateFrom/$lte dateTo via datetime.fromisoformat tolerant of trailing Z) and build_sort(sort_by, sort_order)->str (+/- prefix). Mirror customer_code/query.py exactly.' },
  { k: 'be-serialize', files: ['backend/app/services/jvml_stock/__init__.py','backend/app/services/jvml_stock/serialize.py'], notes: ['be-services-core'], refs: ['backend/app/services/customer_code/serialize.py'],
    job: 'Create services/jvml_stock/__init__.py (empty) and serialize.py to_jvml_stock_public(doc: JvmlStock)->JvmlStockPublic mapping EVERY field (id=str(doc.id)). Copy the field set from ADDENDUM.' },
  { k: 'be-list', files: ['backend/app/services/jvml_stock/list.py'], notes: ['be-services-core'], refs: ['backend/app/services/customer_code/list.py'],
    job: 'Create list.py list_jvml_stock(query: JvmlStockListQuery)->tuple[list[JvmlStockPublic], PaginationMeta]: build_jvml_stock_filter -> count -> find(filt).sort(build_sort).skip().limit() -> to_jvml_stock_public -> PaginationMeta. Mirror customer_code/list.py exactly (incl. how PaginationMeta is built).' },
  { k: 'be-options', files: ['backend/app/services/jvml_stock/options.py'], notes: ['be-services-core'], refs: ['backend/app/services/customer_code/options.py'],
    job: 'Create options.py search_field_options(query: JvmlStockOptionsQuery)->list[AsyncOption] via JvmlStock.distinct(query.field, filt) (Beanie 2.x: filter is 2nd positional arg). filt = {field:{"$regex":re.escape(q),"$options":"i"}} when q else {}. AsyncOption from ...schemas.admin_user. Drop None/empty, sort, cap at query.limit. Mirror customer_code/options.py.' },
  { k: 'be-customer-map', files: ['backend/app/services/jvml_stock/customer_map.py'], notes: ['be-ingest-poller'], refs: ['backend/app/models/customer_code.py'],
    job: 'Create customer_map.py async build_customer_map(normalized_codes: set[str])->dict[str, tuple[str|None,str|None]]: if empty return {}; query CustomerCode.find({"code":{"$in":list(normalized_codes)}}); first match per code wins; value=(doc.customer, str(doc.id)). Confirm CustomerCode field names from the model.' },
  { k: 'be-ingest', files: ['backend/app/services/jvml_stock/ingest.py'], notes: ['be-ingest-poller'], refs: ['backend/app/services/customer_code/import_rows.py'],
    job: 'Create ingest.py async ingest_file(path: str, report_date: str)->int: read bytes; parse_workbook; for each row apply coerce_value per field; party_code_normalized = (party_code or "").lstrip("0") or None; collect normalized codes; build_customer_map; attach customer_name/customer_code_id; set report_date, source_file=path, created_at/updated_at now UTC; idempotent: await JvmlStock.find({"report_date":report_date}).delete() then insert_many in chunks of 1000; emit audit_jvml_stock_event("jvml_stock.ingested", extra={report_date,row_count}); return count. Import audit_jvml_stock_event from ...audit.events.' },
  { k: 'be-poller', files: ['backend/app/services/jvml_stock/poller.py'], notes: ['be-ingest-poller'], refs: [],
    job: 'Create poller.py async run_poll()->JvmlStockStatusPublic implementing the full window/filesystem logic in SPEC §2 (load config; if not enabled return status; validate base_path+file_name; now=datetime.now(); today=%d-%m-%Y; parse start/end HH:MM; if now.time()<start return; folder=os.path.join(base_path,today); os.makedirs(folder, exist_ok=True); get/create JvmlStockIngestion keyed report_date; if status=="ingested" return; fpath=os.path.join(folder, file_name+".xlsx"); if isfile -> ingest_file, mark ingested(row_count,found_at,file_path); else if now.time()>=end and status!="alerted" -> send_missing_alert, mark alerted(alerted_at); else mark missing; wrap ingest in try/except -> mark error + audit jvml_stock.failed; never raise). Return get_status().' },
  { k: 'be-emails', files: ['backend/app/services/jvml_stock/emails.py'], notes: ['be-ingest-poller'], refs: ['backend/app/core/email.py'],
    job: 'Create emails.py render_missing_email(report_date, file_name, base_path)->tuple[str,str,str] (subject "⚠ JVML STOCK EXCEL not found — {report_date}") and async send_missing_alert(config, report_date)->None looping config.notify_emails calling core.email.send_email; emit audit_jvml_stock_event("jvml_stock.missing_alert",...); never raises.' },
  { k: 'be-config-service', files: ['backend/app/services/jvml_stock/config_service.py'], notes: ['be-ingest-poller'], refs: [],
    job: 'Create config_service.py async get_config()->JvmlStockConfigPublic (singleton key="default", defaults if absent) and async upsert_config(body: JvmlStockConfigUpdate, *, actor_email)->JvmlStockConfigPublic (find-or-create singleton, set fields, bump updated_at, save; then call apply_jvml_stock_schedule() imported from ...core.scheduler; emit audit_jvml_stock_event("jvml_stock.config_updated")). Use local import for apply_jvml_stock_schedule to avoid cycles if needed.' },
  { k: 'be-status', files: ['backend/app/services/jvml_stock/status.py'], notes: ['be-ingest-poller'], refs: [],
    job: 'Create status.py async get_status()->JvmlStockStatusPublic: read config.enabled; latest JvmlStockIngestion + recent 14 by created_at desc -> JvmlStockIngestionRow list; populate last_* fields from the most recent ingestion.' },
  { k: 'be-cron', files: ['backend/app/services/cron/jvml_stock.py'], notes: ['be-ingest-poller'], refs: ['backend/app/services/cron/heartbeat.py'],
    job: 'Create services/cron/jvml_stock.py: from .decorators import? NO — audited_job lives in .heartbeat (import: from .heartbeat import audited_job). Define @audited_job("cron.jvml_stock_poll") async def jvml_stock_poll_job()->None: local import run_poll from ...services.jvml_stock.poller; await run_poll(). Confirm audited_job import path from the cron package __init__.' },
  { k: 'be-controllers', files: ['backend/app/controllers/jvml_stock.py','backend/app/controllers/jvml_stock_config.py'], notes: ['be-controller-routes'], refs: ['backend/app/controllers/customer_code.py'],
    job: 'Create controllers/jvml_stock.py (list_jvml_stock_controller + field_options_controller gated by get_current_user; frozenset unknown-key rejection: list allowed={page,limit,sortBy,sortOrder,q,dateFrom,dateTo}+10 filters; options allowed={field,q,limit}; build query schema; call service; return success(rows, meta=pagination)) and controllers/jvml_stock_config.py (get_config_controller, update_config_controller(body, current_admin: AuthUser) -> threads .emailid, get_status_controller, run_now_controller calling run_poll). Mirror customer_code controller signature style EXACTLY (check whether it takes Request and how query is parsed).' },
  { k: 'be-routes', files: ['backend/app/routes/jvml_stock.py'], notes: ['be-controller-routes'], refs: ['backend/app/routes/customer_code.py'],
    job: 'Create routes/jvml_stock.py with TWO exported routers: router=APIRouter(prefix="/jvml-stock", tags=["jvml-stock"], dependencies=[Depends(get_current_user)]) with GET "" (list) and GET "/options" (registered before any param route); config_router=APIRouter(prefix="/admin/jvml-stock", tags=["jvml-stock-admin"], dependencies=[Depends(get_current_admin)]) with GET "/config", PUT "/config", GET "/status", POST "/run-now". Each uses response_model=SuccessEnvelope[...]. Mirror routes/customer_code.py.' },

  // ---------------- FRONTEND (15 builders, 21 files) ----------------
  { k: 'fe-types-stock', files: ['frontend/src/types/jvml-stock/stock.ts'], notes: ['fe-types-api'], refs: ['frontend/src/types/admin/customer-code.ts'],
    job: 'Create stock.ts: JvmlStockSortBy, JvmlStockField unions (exact per ADDENDUM); JvmlStock interface = id + ALL 72 fields (text->string|null, number->number|null, date->string|null) + party_code_normalized, customer_name, customer_code_id (string|null), report_date:string, source_file:string|null, created_at:string, updated_at:string (copy TS block VERBATIM from ADDENDUM); JvmlStockListQuery extends PageQuery with sortBy?, q?, the 10 filters?, dateFrom?, dateTo?. Import PageQuery from the same place customer-code.ts does.' },
  { k: 'fe-types-ui', files: ['frontend/src/types/jvml-stock/stock-ui.ts'], notes: ['fe-types-api'], refs: ['frontend/src/types/admin/customer-code-ui.ts'],
    job: 'Create stock-ui.ts: JvmlStockQueryState, JvmlStockDialogState ({type:"none"}|{type:"view";row:JvmlStock}), prop interfaces for table/toolbar/filters/pagination/view-sheet, UseJvmlStockListResult. Mirror customer-code-ui.ts shapes (pagination prop isLoading, table prop loading).' },
  { k: 'fe-types-config', files: ['frontend/src/types/settings/jvml-stock-config.ts','frontend/src/types/settings/jvml-stock-config-ui.ts'], notes: ['fe-types-api','fe-settings'], refs: [],
    job: 'Create jvml-stock-config.ts (JvmlStockConfig {enabled,base_path,file_name,start_time,end_time,interval_hours,notify_emails:string[],updated_at:string|null}, JvmlStockConfigInput=same minus updated_at, JvmlStockStatus {enabled,last_report_date,last_status,last_row_count,last_found_at,last_alerted_at,last_error,recent:JvmlStockIngestionRow[]}, JvmlStockIngestionRow) and jvml-stock-config-ui.ts (form prop contracts + UseJvmlStockConfigResult).' },
  { k: 'fe-api-list', files: ['frontend/src/api/jvml-stock/list.ts','frontend/src/api/jvml-stock/options.ts'], notes: ['fe-types-api'], refs: ['frontend/src/api/admin/customer-codes/list.ts','frontend/src/api/admin/customer-codes/options.ts'],
    job: 'Create list.ts listJvmlStock(query): Promise<PaginatedResult<JvmlStock>> via getList+buildQuery, forwarding dateFrom/dateTo and the 10 filters, stripping empty/undefined. Create options.ts searchJvmlStockFieldOptions(field): (q)=>Promise<AsyncOption[]> curried factory hitting /jvml-stock/options?field=&q=&limit=20. Mirror customer-codes list/options exactly.' },
  { k: 'fe-api-config', files: ['frontend/src/api/settings/jvml-stock-config/get.ts','frontend/src/api/settings/jvml-stock-config/update.ts','frontend/src/api/settings/jvml-stock-config/status.ts','frontend/src/api/settings/jvml-stock-config/runNow.ts'], notes: ['fe-types-api'], refs: ['frontend/src/api/admin/customer-codes/get.ts'],
    job: 'Create 4 tiny files: get.ts getJvmlStockConfig()->Promise<JvmlStockConfig> (getData "/admin/jvml-stock/config"); update.ts updateJvmlStockConfig(body: JvmlStockConfigInput)->Promise<JvmlStockConfig> (putData "/admin/jvml-stock/config", body); status.ts getJvmlStockStatus()->Promise<JvmlStockStatus> (getData "/admin/jvml-stock/status"); runNow.ts runJvmlStockCheckNow()->Promise<JvmlStockStatus> (postData "/admin/jvml-stock/run-now", {}). Import helpers from @/api/client (putData is added by orchestrator).' },
  { k: 'fe-table', files: ['frontend/src/components/jvml-stock/JvmlStockTable.tsx'], notes: ['fe-table-pagination'], refs: ['frontend/src/components/admin/customer-codes/CustomerCodeTable.tsx'],
    job: 'Create JvmlStockTable.tsx: sortable server table, columns Report Date · Party Code · Customer · Customer Name (italic muted — when null) · Sold To Party · Material · JSW Grade · Sales Office · Distr.Chnl · Unrestr Qty (tabular-nums) · Stock Qty · view action. Sort only on whitelist columns. Loading skeleton + empty (Boxes icon) + error states. Mirror CustomerCodeTable.tsx. <=250 lines.' },
  { k: 'fe-pagination', files: ['frontend/src/components/jvml-stock/JvmlStockTablePagination.tsx'], notes: ['fe-table-pagination'], refs: ['frontend/src/components/admin/customer-codes/CustomerCodeTablePagination.tsx'],
    job: 'Create JvmlStockTablePagination.tsx mirroring CustomerCodeTablePagination.tsx (prop isLoading; singular/plural label "stock row"/"stock rows").' },
  { k: 'fe-toolbar', files: ['frontend/src/components/jvml-stock/JvmlStockTableToolbar.tsx'], notes: ['fe-toolbar-filters'], refs: ['frontend/src/components/admin/customer-codes/CustomerCodeTableToolbar.tsx'],
    job: 'Create JvmlStockTableToolbar.tsx: free-text search input (debounced, sets q) + DateRangePicker (dateFrom/dateTo, createdAt) + JvmlStockFilters popover trigger with active-count badge. Use exact DateRangePicker prop names confirmed in notes. Mirror CustomerCodeTableToolbar.tsx.' },
  { k: 'fe-filters', files: ['frontend/src/components/jvml-stock/JvmlStockFilters.tsx'], notes: ['fe-toolbar-filters'], refs: ['frontend/src/components/admin/customer-codes/CustomerCodeFilters.tsx'],
    job: 'Create JvmlStockFilters.tsx: shadcn Popover with a 2-col grid of 10 AsyncCombobox filters (one per JvmlStockField), module-level frozen FETCHERS built from searchJvmlStockFieldOptions(field), plus Clear all. Active count badge on trigger. Use exact AsyncCombobox prop names from notes. Mirror CustomerCodeFilters.tsx. <=250 lines.' },
  { k: 'fe-fields-config', files: ['frontend/src/components/jvml-stock/jvml-stock-fields.ts'], notes: ['fe-viewsheet'], refs: [],
    job: 'Create jvml-stock-fields.ts: export a config array grouping ALL 72+meta fields into 9 labelled groups (Order & SO · Customer & Mapping · Material & Dimensions · Quantities · Quality/NCO · Chemistry (%) · Mechanical · Logistics/Export · Meta) as {group: string; fields: {key: keyof JvmlStock; label: string; kind: "text"|"number"|"date"|"datetime"}[]}[]. Copy the grouping from ADDENDUM if present. Every JvmlStock field must appear exactly once.' },
  { k: 'fe-viewsheet', files: ['frontend/src/components/jvml-stock/ViewJvmlStockSheet.tsx'], notes: ['fe-viewsheet'], refs: ['frontend/src/components/admin/customer-codes/ViewCustomerCodeSheet.tsx'],
    job: 'Create ViewJvmlStockSheet.tsx: shadcn Sheet (side right, scrollable) rendering the 9 groups from jvml-stock-fields.ts via a generic <Field label value> renderer (— for null; date/datetime via date-fns). Mirror ViewCustomerCodeSheet.tsx. <=250 lines.' },
  { k: 'fe-hook', files: ['frontend/src/components/jvml-stock/hooks/useJvmlStockList.ts'], notes: ['fe-hook-page'], refs: ['frontend/src/components/admin/customer-codes/hooks/useCustomerCodeManagement.ts'],
    job: 'Create useJvmlStockList.ts: owns query state {page,limit,sortBy,sortOrder,q,10 filters,dateFrom,dateTo}, rows/meta/loading/error, dialog union; setters setPage/setLimit/setSort/setSearch/setFilter/setDateRange/clearFilters/refetch/openDialog/closeDialog; race-safe fetchIdRef calling listJvmlStock. Mirror useCustomerCodeManagement.ts.' },
  { k: 'fe-page', files: ['frontend/src/pages/jvml-stock/index.tsx'], notes: ['fe-hook-page'], refs: ['frontend/src/pages/admin/customer-codes/index.tsx'],
    job: 'Create pages/jvml-stock/index.tsx default-exporting JvmlStockListPage: header (icon chip Boxes + "JVML Stock List" + subtitle), JvmlStockTableToolbar, JvmlStockTable, JvmlStockTablePagination, ViewJvmlStockSheet — thin orchestrator wired to useJvmlStockList. Mirror customer-codes page. Default export.' },
  { k: 'fe-settings-status', files: ['frontend/src/components/settings/JvmlStockConfigStatus.tsx'], notes: ['fe-settings'], refs: [],
    job: 'Create JvmlStockConfigStatus.tsx: compact status panel taking JvmlStockStatus — enabled badge, last run report_date/status/row_count, last_found_at, last_error. Use shadcn Badge + semantic tokens. <=250 lines.' },
  { k: 'fe-settings-card', files: ['frontend/src/components/settings/JvmlStockConfigCard.tsx','frontend/src/components/settings/hooks/useJvmlStockConfig.ts'], notes: ['fe-settings'], refs: ['frontend/src/components/admin/regions/CreateRegionDialog.tsx','frontend/src/components/admin/regions/EmailChipInput.tsx'],
    job: 'Create useJvmlStockConfig.ts (load config+status on mount, form state, save() via updateJvmlStockConfig, runNow() via runJvmlStockCheckNow, loading/saving/error flags, sonner toasts) and JvmlStockConfigCard.tsx (Card "JVML Stock Excel Configuration" + helper "<base_path>/<dd-mm-yyyy>/<file_name>.xlsx"; Switch enabled; Input base_path; Input file_name (+.xlsx hint); start/end Input type="time"; interval Select 1..24 labelled "Every N hour(s)"; reuse @/components/admin/regions/EmailChipInput for notify_emails; footer "Run check now" (secondary) + "Save configuration" (primary, aria-busy); render <JvmlStockConfigStatus> below; Field/FieldLabel/FieldError; client validate start<end & file_name has no / \\\\ separators). Both <=250 lines.' },
]

phase('Build')
const built = await parallel(T.map((t) => () =>
  agent(
    `You are builder "${t.k}" for the JVML Stock feature.\n${GUARD}\n\nCREATE EXACTLY THESE FILE(S):\n- ${t.files.join('\n- ')}\n\nPRIMARY NOTES: ${t.notes.map((n) => `${NOTES}/${n}.md`).join(', ')}\nREFERENCE FILE(S) to mirror: ${t.refs.length ? t.refs.join(', ') : '(none — derive from SPEC/ADDENDUM/notes)'}\n\nTASK: ${t.job}`,
    { label: t.k, phase: 'Build' }
  )
))

const ok = built.filter(Boolean).length
log(`Builders done: ${ok}/${T.length}`)
return { builders: ok, total: T.length, files: T.flatMap((t) => t.files) }
